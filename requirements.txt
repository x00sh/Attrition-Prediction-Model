# Runtime dependencies for the attrition prediction UI (app.py + src/).
# The model/scaler artifacts were pickled with scikit-learn 1.7.x; loading them
# under a newer minor version works (parity test passes exactly) but prints an
# InconsistentVersionWarning. Pin to 1.7.* if you want to silence it.
streamlit>=1.30
scikit-learn>=1.7
pandas>=2.0
numpy>=1.24
joblib>=1.3
shap>=0.44
matplotlib>=3.7
pyarrow>=12.0
