"""Attrition prediction UI support package.

`preprocess` reproduces the training-time raw -> 56-feature transform; `predict`
loads the deployed model and returns a Leave/Stay verdict plus SHAP drivers.
"""
